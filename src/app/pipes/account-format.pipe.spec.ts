import { AccountFormatPipe } from './account-format.pipe';

describe('AccountFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
